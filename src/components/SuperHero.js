import React from 'react';

export default function SuperHero(props) {
    return (
        <div>
            <h1>Hero's name is {props.Name} and His Stage Name is {props.HeroName} </h1>
        </div>
    )
}
